# Admin features

Keep each dashboard domain self-contained here. Start with `members`,
`attendance`, `events`, `departments`, and `announcements` as each feature is
implemented. Route files belong in `src/app`; reusable domain UI and logic
belong in the corresponding feature folder.
