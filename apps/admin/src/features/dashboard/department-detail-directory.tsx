import type{PageData}from"./queries";import{DashboardPagination}from"./dashboard-pagination";
const initials=(name:string)=>name.split(/\s+/).slice(0,2).map(word=>word[0]).join("").toUpperCase();
export function DepartmentDetailDirectory({data}:{data:PageData}){return <><section className="leaders-panel"><div className="panel-heading"><div><h2>All leaders</h2><p>Current and historical department leadership.</p></div></div><div className="table-scroll" tabIndex={0} aria-label="Scrollable department leaders"><table className="data-table"><thead><tr><th scope="col">Leader</th><th scope="col">Title</th><th scope="col">Tenure</th><th scope="col">Status</th></tr></thead><tbody>{(data.leaders||[]).map(row=><tr key={row.id}><td><div className="identity-cell"><div className="initials">{initials(row.primary)}</div><div><strong>{row.primary}</strong>{row.secondary&&<span>{row.secondary}</span>}</div></div></td><td>{row.details?.title_name||"—"}</td><td>{row.details?.start_date||"—"}{row.details?.end_date?` — ${row.details.end_date}`:" — Present"}</td><td><span className="status-pill">{row.status}</span></td></tr>)}</tbody></table></div>{!data.leaders?.length&&<p className="modal-empty">No leaders found.</p>}</section><section className="directory-panel"><div className="panel-heading"><div><h2>Department members</h2><p>Searchable authorised member directory.</p></div></div><div className="table-scroll" tabIndex={0} aria-label="Scrollable department members"><table className="data-table members-table"><thead><tr><th scope="col">Membership code</th><th scope="col">Member</th><th scope="col">Phone</th><th scope="col">Joined</th><th scope="col">Status</th></tr></thead><tbody>{data.rows.map(row=><tr key={row.id}><td>{row.details?.membership_code||"—"}</td><td><div className="identity-cell"><div className="initials">{initials(row.primary)}</div><div><strong>{row.primary}</strong>{row.secondary&&<span>{row.secondary}</span>}</div></div></td><td>{row.details?.phone||"—"}</td><td>{row.meta}</td><td><span className="status-pill">{row.status}</span></td></tr>)}</tbody></table></div>{!data.rows.length&&<p className="modal-empty">No members match this search.</p>}{data.pagination&&<DashboardPagination {...data.pagination} label="Department member directory pages"/>}</section></>;}
